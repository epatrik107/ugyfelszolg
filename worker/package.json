{
  "name": "@ugyfelkozpont/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "npx wrangler dev",
    "predeploy": "node ../scripts/worker-deploy-preflight.mjs",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.9.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260518.0",
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
